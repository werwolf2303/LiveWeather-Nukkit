package com.liveweather.server;

@SuppressWarnings("unused")
public class PleaseRead {


    //ENGLISH
    // The code in the package 'server' is for the webserver that i want to code in. But it doesn't work so good. Maybe in the next updates
    // Enable it with: 'configserver:true'
    // Set Password with: 'configserverpassword:YOUR_PASSWORD'
    // options.cfg


    //DEUTSCH
    // Der Code in package 'server' ist für einen Konfigurationsserver den ich einprogrammieren will aber er funktioniert nicht so gut und hat Fehler. In den nächsten Updates vielleicht
    // Aktiviere ihn in dem du 'configserver:true' und das Passwort mit 'configserverpassword:DEIN_PASSWORT' in options.cfg setzt
}
